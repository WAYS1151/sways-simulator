import React from "react";
import ReactDOM from "react-dom/client";
import SwaysSimulator from "./SwaysSimulator";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SwaysSimulator />
  </React.StrictMode>
);
